#include <stdio.h>
#include <stdlib.h>

void main() {

  /* condicionais são instrucoes que ocorre dada uma condicao verdadeira
   * 
   * comparadores 
   *
   * ==  - igual
   * != - diferente
   * > - maior
   * < - menor
   * >= - maior ou igual
   * <= - menor ou igual
   * true - verdadeiro
   * false- falso
   * 
   * nos casos de caracteres a comparacao se da entre seu valor na tabela asci
   * */

  /* condicional if/else/elseif
   *
   * 
   * no caso da condicional if, o c ira executar um comando se a condicao passada no if for
   * verdadeira 
   *
   * estrutura
   *  if(condicao){
   *   // codigo
   *  }
   *
   * if/else
   * caso a condição não seja satisfeita podemos executar outro bloco com o else
   *
   * estrutura
   *  if(condicao) {
   *    //codigo
   *  } else{
   *  }
   *
   * else if
   *
   * igual if porem se uma condicao for verdadeira ignora o resto
   *
   * estrutura
   * if(condicao) {
   *  //codigo
   * } else if(codicao){ 
   *     //codigo    
   * }else {
   *  //codigo
   * }
   *
   * condicional switch case
   *
   * estrutura
   * switch(variavel){
   *  case condicao:
   *    break;
   *  case condicao2:
   *    break;
   *}
   *  o switch pega uma variavel e verifica o valor dela
   *  caso seja alguma condicao que voce escolhei ele ira
   *  reproduzir o trecho de codigo ate o break que
   *  encerra o switch, podemos utilizar o continue
   *  no lugar do switch , porem ele continuara verificando
   *  as outras condicoes
   *
   *
   *
   *  */
}
