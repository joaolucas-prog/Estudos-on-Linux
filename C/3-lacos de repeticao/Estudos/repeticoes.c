#include <stdio.h>
#include <stdlib.h>

void main() {
  /* 
   A estrutura de repeticao serve para o programa repetir o bloco
   de codigo enquanto determinada condicao e verdadeira
  
   While:
    -estrutura
      while(condicao) {
        (trecho de codigo)
      }

    -exemplo
    int value= 0;
    while(value <= 9){
      printf("%d",value);
      value = value + 1;
    }
    
  do while
    -estrutura:
    do {
      (trecho de codigo)
    }while(condicao);

  diferença entre dowhile x while:
    o do while irá executar o codigo  primeiro e depois verifica a condicao
    enquanto o while apenas executa o codigo depois de verificar a condicao
  
  for
    -estrutura:
    for(variavel de entrada ; condicao ; incremento) {
      (trecho de codigo)
    }

    -exemplo:

    for(int i=0;i<=10;i++){
      printf("%d",i);
    }

    Constantes
      contantes são valores que nunca mundam, para criar uma constante
      devemos no inicio do documento escrever #define nomeContante valorConstante
      ex: #define TAMANHO 10
*/
  
}
